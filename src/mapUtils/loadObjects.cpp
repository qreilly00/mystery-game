#include "../MapUtils.hpp"

/*std::vector<LevelShape> MapUtils::loadObjects() {
    /*std::ifstream ifs;
    ifs.open("example.txt");

    while(ifs.eof() != 1;) {
        if() {

        }
    }

    for(auto x : objects) {
        sf::RectangleShape tmp = x.getShape();
        ofs << "(" << tmp.getSize().x << "," << tmp.getSize().y << ")" <<
            << "(" << tmp.getFillColor() << ")" <<
            << "(" << tmp.getIsCollidable() << ")" <<
            << "(" << tmp.getLevel() << ")" <<
            << "(" << tmp.getStage() << ")" << std::endl;
    }*

    return LevelShape();
}*/
